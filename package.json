{
  "name": "composerx",
  "version": "0.0.9",
  "main": "dist/min/index.js",
  "repository": {
    "type": "git",
    "url": "https://fedeghe@github.com/fedeghe/composerx.git"
  },
  "keywords": [
    "compose regexp",
    "compose regex",
    "regexp composer",
    "regex composer",
    "regexp",
    "regex",
    "regular expressions",
    "regular expression",
    "regular expressions composer",
    "regular expression composer"
  ],
  "author": "Federico Ghedina <fedeghe@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.27.0",
    "coveralls": "^3.1.1",
    "eslint": "^9.27.0",
    "malta": "^4.1.42",
    "malta-header-comment": "^1.0.12",
    "malta-jest": "^0.0.1",
    "malta-millijs": "^1.0.5"
  },
  "scripts": {
    "dev": "malta source/buildev.json",
    "build": "malta source/build.json",
    "test": "jest  --verbose --rootDir=tests",
    "watch": "jest --watch --rootDir=tests",
    "cover": "jest --collectCoverage --rootDir=tests",
    "coveralls": "jest --collectCoverage --rootDir=$PWD/tests && cat ./tests/coverage/lcov.info | coveralls",
    "lint": "eslint source"
  }
}
